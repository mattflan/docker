---
version: "2"
services:
  homer:
    image: b4bz/homer
    container_name: homer
    volumes:
      - /srv/homer/data:/www/assets
    ports:
      - 880:880
    #environment:
    #  - UID=1000
    #  - GID=1000
    restart: unless-stopped
